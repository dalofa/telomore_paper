Fasta files used to generate clustering. All of G1034, NCBI, known telomers, Wellington. 
Note that circular sequence 'end' are also contained in the files and was /can be used a a clustering control: they shouldn't cluster nicely, which they also don't
